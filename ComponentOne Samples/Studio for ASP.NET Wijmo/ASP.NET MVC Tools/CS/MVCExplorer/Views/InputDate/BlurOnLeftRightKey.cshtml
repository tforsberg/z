@{
    ViewBag.Title = "Blur On Left Right Key";
    Layout = "~/Views/Shared/_WidgetLayout.cshtml";
}
@section header
{
	<script id="scriptInit" type="text/javascript">
		$(document).ready(function () {
		    $("#textbox2").wijinputdate({
		        blurOnLeftRightKey: "both"
		    });
		    $("#textbox1").wijinputtext({
		    });
		    $("#textbox3").wijinputtext({
		    });
		});
	</script>
}
 <h2>Blur On Left Right Key</h2>
<div class="main demo">
            <!-- Begin demo markup -->
            <input type="text" id="textbox1" />
            <input type="text" id="textbox2" />
             <input type="text" id="textbox3" />
            <!-- End demo markup -->
            <div class="demo-options">
                <!-- Begin options markup -->
                <!-- End options markup -->
            </div>
</div>
@section description
{
  <p>
                This sample demonstrates that when the <b>blurOnLeftRightKey</b> option is set to <b>both</b>,
                for the date widget, when caret is in the first field, focus will jump to the previous control when press left key, 
                when caret is in the last field, focus will just to the next control when press right key.</p>
}


