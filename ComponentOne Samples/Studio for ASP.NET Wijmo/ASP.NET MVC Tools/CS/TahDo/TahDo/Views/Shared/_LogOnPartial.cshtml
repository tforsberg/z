@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@Context.User.Identity.Name</strong>!
    @Html.ActionLink("Log Off", "LogOff", "Account", null, new { @class = "button" })</text>
}
else {
    @:@Html.ActionLink("Log On", "LogOn", "Account", null, new { @class = "button" })
}
